export interface AppInfo {
  user_id: number;
  name: string;
  url: string;
  desc: string;
  callback: string;
  success_url: string;
  cancel_url: string;
  logo: string;
  uuid: string;
  active: number;
  enabled: number;
  card: number;
  created_at: string;
  updated_at: string;
  app_photo_url: string;
}
